struct Shape {
    //@height: int;
    //@width: int;
}

struct Person {
    @name: string;
    @age: int;
    @height: string;
    @weight: float;
}

func init_shape(@my_shape: Shape, @height: int, @width: int): none {
    @my_shape.height = @height;
    @my_shape.width = @width;
}

func init_person(@my_person: Person, @name: string, @age: int, @height: string, @weight: float): none {
    @my_person.name = @name;
    @my_person.age = @age;
    @my_person.height = @height;
    @my_person.weight = @weight;
}

func main(): none {
    print("working on struct");
    
    // initializer list for variable decl of type struct
    // @square, @rectangle -> (Shape);
    // @matt: Person;
    
    //init_person(@matt, "matt", 29, "6 feet", 240);
    //init_shape(@square, 10, 10);
    //init_shape(@rectangle, 10, 15);
}