func main(): none {
    // these are the 
    // variable declarations, then init 
    @my_int: int;
    @my_float: float;
    @my_bool: bool;
    @my_null_var: none;
    @my_string: string;
    @my_int = 40;

    // declare and init at same time
    @x: float = 500.50;

    //initializer lists
    @arg1, @arg2, @arg3 -> (float, 0.0);
}